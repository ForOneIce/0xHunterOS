
import React from 'react';

// This component is deprecated and replaced by TargetManager.
// Keeping file to avoid build errors if referenced elsewhere, but rendering nothing.
const DailyBriefing: React.FC<any> = () => {
  return null;
};

export default DailyBriefing;
